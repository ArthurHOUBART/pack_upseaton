define command {
       command_name     check_upseaton
       command_line     $PLUGINSDIR$/check_snmp_mgeeaton_ups.pl -H "$HOSTADDRESS$" -C "$_HOSTUPSEATON_COMMUNITY$" -t $_HOSTUPSEATON_TIMEOUT$ -O $ARG1$
}
