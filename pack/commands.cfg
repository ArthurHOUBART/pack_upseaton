define command {
       command_name     check_upseaton_outputload
       command_line     $PLUGINSDIR$/check_snmp_mgeeaton_ups.pl -H "$HOSTADDRESS$" -C "$_HOSTUPSEATON_COMMUNITY$" -t $_HOSTUPSEATON_TIMEOUT$ -O output_load 
}
define command {
       command_name     check_upseaton_batteryremainingtime
       command_line     $PLUGINSDIR$/check_snmp_mgeeaton_ups.pl -H "$HOSTADDRESS$" -C "$_HOSTUPSEATON_COMMUNITY$" -t $_HOSTUPSEATON_TIMEOUT$ -O battery_remaining_time
}
